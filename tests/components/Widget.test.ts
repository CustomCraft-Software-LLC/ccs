import { Widget } from '../../packages/components/src';
import React from 'react';
import { render } from '@testing-library/react';

describe('Widget', () => {
    test('Widget', () => {
        
    });

});