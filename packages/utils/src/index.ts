import { validateEmail } from './utils/validation';
export { HTML_TAGS, checkTagExists } from './constants';
export { 
    validateContactForm, 
    validateEmail, 
    validatePassword, 
    validatePhoneNumber, 
    parseJSON, 
    removeCookie, 
    formatText, 
    sortTable, 
    setCookie 
} from'./utils';