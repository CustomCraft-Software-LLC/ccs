import React from "react";
interface HeroDesktopProps {
    heading: string;
    subheading: string;
    image: string;
    alt?: string;
}
declare const HeroDesktop: React.FC<HeroDesktopProps>;
export default HeroDesktop;
